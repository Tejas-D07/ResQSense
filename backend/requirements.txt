# Predictive Emergency AI — Python dependencies
# Install with: pip install -r requirements.txt

# Core ML — YAMNet model
tensorflow>=2.13.0
tensorflow-hub>=0.14.0

# Audio recording (16kHz microphone input)
sounddevice>=0.4.6

# SSL/TLS certificate bundle for secure model download
certifi>=2024.12.0

# Numerical computation
numpy>=1.24.0
